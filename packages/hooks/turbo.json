{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@react-vant-next/utils#build"],
      "outputs": ["dist/**", "es/**", "lib/**"],
      "cache": true,
      "inputs": ["src/**", "package.json", "rollup.config.js", "rollup.config.ts", "tsconfig.json"]
    }
  }
}
